{
  "name": "finally-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E suite for FinAlly, run against the containerized app.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
